# Agent boundary example

The TypeScript interfaces in `packages/public-agent-interfaces/` describe a
small boundary between a role-specific agent and a structured result. The
demo uses fixed synthetic values; it does not call a model or service.
