[project]
name = "mef-workspace"
version = "0.1.0"
requires-python = "==3.12.6"
dependencies = []

[dependency-groups]
dev = [
  "cyclonedx-bom==7.3.1",
  "detect-secrets==1.5.0",
  "hatchling==1.32.0",
  "jsonschema==4.26.0",
  "pip-audit==2.10.1",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = [
  "packages/generated-python",
  "scientific/kernel",
  "tools/evidence-replay"
]
